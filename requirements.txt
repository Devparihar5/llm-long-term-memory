# Core dependencies
langchain>=0.1.0
langchain-openai>=0.0.5
openai>=1.0.0
faiss-cpu>=1.7.4
numpy>=1.24.0
pydantic>=2.0.0

# Streamlit UI
streamlit>=1.30.0

# Optional database backends (install as needed)
# pip install psycopg2-binary  # PostgreSQL
# pip install pymongo          # MongoDB  
# pip install redis            # Redis
